const db = require("../connection/connection")
const cron = require('node-cron')




















module.exports = {
    add_intern_task, show_intern_task, task_file_upload, show_specific_intern_project, status_update
}